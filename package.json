{
  "name": "schema-editor",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --minify --outfile=dist/bundle.js --format=iife",
    "watch": "esbuild src/main.ts --bundle --outfile=dist/bundle.js --format=iife --watch",
    "typecheck": "tsc --noEmit",
    "test": "node --test --import tsx/esm tests/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "esbuild": "^0.27.4",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
